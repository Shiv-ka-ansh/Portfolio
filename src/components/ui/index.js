/**
 * UI Components Barrel Export
 * 
 * This file provides a single import point for all UI primitives.
 * These are atomic, reusable components that don't know about
 * portfolio-specific content.
 * 
 * Usage:
 * import { SectionShell, SectionTitle, Timeline } from '@/components/ui';
 */

export { default as SectionShell } from './SectionShell';
export { default as SectionTitle } from './SectionTitle';
export { default as Timeline } from './Timeline';
export { default as GlowCard } from './GlowCard';
export { default as FloatingOrbs } from './FloatingOrbs';
export { HeroGeometric, ElegantShape } from './HeroGeometric';

